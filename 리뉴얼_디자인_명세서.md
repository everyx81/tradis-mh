# TRADIS MH 리뉴얼 — 디자인 명세서 (Design Spec)

> 이 문서는 TRADIS MH 디자인 리뉴얼의 **구현 설계도**입니다.
> 시안(목업)에서 합의한 디자인을 PyQt6 코드로 옮길 때 이 문서를 기준으로 작업합니다.
> 작성: 2026-06-28 · 대상: 정산(Settlement) 탭 우선

---

## 0. 개요

| 항목 | 내용 |
|------|------|
| 목표 | **디자인/UX 리뉴얼** — 전체 디자인 구조 통일 + 편의성 개선 (재작성 ❌, 구조 갈아엎기 ❌) |
| 유지 | Windows PyQt6 데스크톱 앱, .exe 배포, **팀원 3명** 규모 |
| 핵심 방향 | 차분한 다크 톤으로 **통일** · 옛 네온(JARVIS) 제거 · **콤팩트 · 심플 · 한눈에** |
| 현재 문제 | 두 테마 혼재(네온 `styles.py` + 다크 `claude_theme.py`), 색상값 200곳 하드코딩 → 화면마다 제각각 |
| 진행 원칙 | **충분히 논의 후 구현**, 화면 하나씩 안전하게 점진 적용 |

---

## 1. 디자인 원칙

1. **하나의 디자인 언어** — 모든 화면(팝업·메뉴 포함)이 같은 룩. 옛 청록 네온 전부 제거.
2. **단일 관리 지점(디자인 토큰)** — 색·폰트·간격을 `claude_theme.py` 한 곳에서 관리. 하드코딩 금지.
3. **포인트 색 하나** — 블루 `#4ba3f7` 만 강조색. 나머지는 회색조 → 정보가 튐.
4. **얇은 헤어라인 · 평면** — 굵은 테두리·그림자·글로우 제거, 1px 헤어라인.
5. **콤팩트** — 군더더기(장식·여백)를 먼저 걷어내고 간격을 촘촘히. 단, 글자 가독성은 유지.
6. **한눈에** — 상태는 색점·작은 알약으로, 누락은 즉시 보이게.

---

## 2. 디자인 토큰 (Single Source of Truth)

> 출처: `gui/claude_theme.py` 의 `C` 딕셔너리. 시안은 전부 이 값을 사용했음. **신규 색 거의 없음.**

### 2.1 배경
| 토큰 | 값 | 용도 |
|------|------|------|
| `bg_0` | `#0d0f15` | 최외곽 창 배경 |
| `bg_1` | `#14171d` | 패널 |
| `bg_2` | `#1b1e24` | 카드 |
| `bg_3` | `#23262d` | 엘리베이티드/호버 |
| `bg_4` | `#2d3038` | 강한 호버 |
| `logs_bg` | `#07090e` | 로그/어두운 영역 |

### 2.2 테두리 / 전경(텍스트)
| 토큰 | 값 | 용도 |
|------|------|------|
| `border_soft` | `rgba(45,48,56,.59)` | 기본 헤어라인 |
| `border` | `rgba(63,66,75,.7)` | 강조 구분선 |
| `border_strong` | `#484b54` | 강한 구분 |
| `fg_0` | `#f8fafc` | 주요 텍스트 |
| `fg_1` | `#d5d9df` | 보조 텍스트 |
| `fg_2` | `#9ea1a8` | 3차 텍스트 |
| `fg_3` | `#6a6e76` | 흐린 텍스트/플레이스홀더 |

### 2.3 액센트 / 상태색
| 토큰 | 값 | 용도 |
|------|------|------|
| `accent` | `#4ba3f7` | 강조(버튼·선택·링크) |
| `accent_hi` | `#5ebdff` | 밝은 강조/아이콘 |
| `accent_lo` | `#3284d0` | 눌림 |
| `green` | `#59c886` | 완료 |
| `amber` | `#e9ab2b` | 대기/확인필요 |
| `red` | `#fa6863` | 충돌/오류 |
| `purple` | `#b48df4` | 수출 태그 등 보조 |
| 트래픽 라이트 | `#ff5f57` / `#febc2e` / `#28c840` | 창 닫기/최소화/최대화 |

### 2.4 폰트 / 라운드
| 토큰 | 값 |
|------|------|
| `FONT_UI` | `Pretendard, Malgun Gothic, Segoe UI` (기본 10pt) |
| `FONT_MONO` | `JetBrains Mono, Consolas, D2Coding` (BL·파일명·금액) |
| `R_SM / R_MD / R_LG / R_XL` | `6 / 10 / 14 / 18` px |

### 2.5 간격 규칙 (콤팩트)
- 카드 안쪽 패딩: 약 10–14px
- 행 높이: 약 32–34px (얇은 리스트)
- 칩/알약 패딩: `2–4px × 8–10px`
- 요소 간 간격: 6–8px

---

## 3. 전역 레이아웃

### 3.1 창 (Window)
- 프레임리스 + 둥근 모서리(`R_LG`), 1px `border`.
- **기본 크기 축소**: 현재 `1700×950` → 더 콤팩트하게(예: `1360×860` 검토). 최소 `1280×720` 유지.
- 적용 위치: `gui_jarvis.py` `resize()` / `setMinimumSize()`.

### 3.2 타이틀바 (height 30–32px)
- 좌측: macOS 트래픽 라이트 3개(11px) → 브랜드 `T RADIS MH`.
- **`T` = 실제 로고 마크** (그라데이션 둥근 사각형, `accent_hi→accent_lo`, 흰 `T`). 출처 `gui_jarvis.py` `brand_mark`(~1610).
- **`T` 클릭 = 실시간 감시 시작/중지** → 아이언맨 눈처럼 글로우(맥동) + `● 감시중` 표시. 다시 클릭 시 꺼짐.
  - 구현: `QGraphicsDropShadowEffect` + `QPropertyAnimation`(앱에 이미 사용 중인 패턴).
- 우측: 버전 표기.

### 3.3 네비 레일 (우측 세로, 폭 44–58px)
- 5탭: **정산 · 일정 · 통관 · 보고 · 설정** (아이콘 + 작은 라벨).
- 활성 탭: `accent_bg` 배경 + `accent_hi` 색.
- 등급: 관리자=5탭 전부 / 일반=정산·설정만 (`license_tier` 유지).
- 적용 위치: `gui_jarvis.py` `_create_vertical_navbar`.

---

## 4. 정산(Settlement) 탭 — 상세 명세 ★

> 정산 탭이 이번 리뉴얼의 핵심. 구조: **[중앙: 그룹/파일] · [우측: 핵심 패널] · [네비]**

### 4.1 상단 바
- **`그룹 / 파일` 토글**(세그먼트): 그룹=카드 리스트, 파일=탐색기형 파일 리스트.
- 그룹 개수 배지.
- ❌ 제거됨: 전체/미비 필터칩, 감시 토글(→ T 로고로 이동), 상태 필터칩 줄(상태는 행에서 표시).

### 4.2 그룹 리스트 — 얇은 행 (List Row)
한 그룹 = 한 줄. 구성(좌→우):
`[상태 색점 7px] · [회사명] · [수입/수출/독립 태그] · [BL (mono, 회색)] · [N/N] · [상태 알약] · [› chevron]`

- **상태 색점/알약**: 🟢완료(`green`) / 🟡대기(`amber`) / 🔴충돌(`red`) / ⚪조치 대기·미분류(`fg_3` 회색).
- **방향 태그**: 수입=`accent` 블루 + ↓ / 수출=`purple` + ↑ / 독립=회색 + 링크 아이콘.
- 호버: `bg_3` 살짝.
- **행 클릭 → 카드 팝업**(아래) / **행 우클릭 → 카드 전체 삭제 메뉴**.
- 적용 위치: `gui/dialogs.py` GroupCard/IndependentCard → 얇은 행 형태로 재구성. (실제 펼침은 팝업으로)

### 4.3 카드 팝업 (Popup Modal) — 공통
- 행 클릭 시 **가운데 팝업**으로 열리고 **뒷배경 어둡게**(`rgba(6,8,13,.66)`). 인라인 펼침 → 팝업으로 변경.
- 구현: `QDialog`(프레임리스, 둥근) + 반투명 오버레이. (진짜 블러는 선택; 어둡게만으로 충분)
- 헤더 공통: `[ID 알약: 상태점+BL] · [회사명] · [N건] · [액션 버튼] · [✕]`.

### 4.4 카드 팝업 — **수입 (정산서 있음)** ⭐ 병합 + 검증
구성(위→아래):
1. **징수형태 배지**(예: `징수형태 14`) + (해당 시) **`월납업체` 배지(amber)**.
2. **필요 서류 체크리스트** 칩: ✓있음(green) / ✗누락(red 점선) / 해당없음(회색 취소선). 끝에 **요약 칩**(`N개 항목 완료` / `N개 중 M개 미확인`).
3. **📎 추가 파일(마킹) 섹션**: 첨부 파일 칩 + ❌ 제거 + `파일/폴더 추가`.
4. **병합 순서 · 금액 대조** 매핑:
   - 슬롯 행: `[번호][슬롯명][파일 드롭다운][▲▼ 순서변경][🔍 미리보기]`
   - 비용 항목 행: `[✓/✗][➡ 비용: 항목명][금액]` — 매칭=파랑 `#64b5ef` / 불일치=빨강 `#ff7e7e`.
   - 병합 순서: **정산서 → 신고필증 → 납부고지서 → 세금계산서 → 비용계산서** (`core/config.py:17`).
5. **금액 검증 배너**(§4.7) — 녹/노/빨.
6. 하단: `＋ 행 추가` · **`합치기 실행`(주 버튼, 블루)**.
- 액션 버튼 라벨(상태머신): `분석 중…` → `펼치기`/`재분석` → `접기`.
- 구현: 기존 `mapping_widget`/`refresh_mapping_ui`/`execute_merge` 재스타일.

### 4.5 카드 팝업 — **수출 (정산서 없음, archive-only)**
- 필요 서류: 수출신고필증/반송신고필증 1개 + `(정산서 없음 → 바로 폴더 정리)`.
- 파일 목록.
- 액션 버튼: **`폴더 정리`** (헤더). **병합·금액검증 없음** 명시.
- 구현: `_archive_export_only`.

### 4.6 카드 팝업 — **독립 카드 (IndependentCard)**
- 단독 BL 문서(예: **이체증**). 회색 `조치 대기`.
- **체크리스트·병합·검증 전부 없음.** 안내: "외부 앱(카톡·메일)으로 끌어다 사용".
- 파일 목록 + 힌트 `↑ 드래그하여 사용 · 우클릭으로 관리`.
- 드래그 아웃(OLE) = 외부 앱으로 파일 전달.
- 적용 위치: `gui/dialogs.py:324` IndependentCard.

### 4.7 금액 검증 (2단계)
- 출처: `core/validator.py validate_mapping_amounts` + `dialogs._run_amount_validation`.
- ① 항목별 1:1 (정산서 비용 항목 금액 vs 계산서 OCR 금액, 동의어 매칭) · ② 합산 비교(오차 10원).
- 배너 문구(실제):
  - 🟢 `항목별/합산 금액 검증 완료`
  - 🟡 `{항목} 금액 불일치 (합산은 일치)` / `합산 불일치 (차이: N원)`
  - 🔴 `{항목} 금액 불일치, 합산 차이 N원`
- 해당없음(NA): 징수형태 14/43 수입세금계산서, 월납업체 수수료 → 검증 제외.

### 4.8 우클릭 메뉴 · 미리보기 · 파일 단축키
- **헤더 우클릭**: `🗑️ 카드 전체 삭제 (N개 파일)` (+ 확인창).
- **파일 우클릭**: `📋 경로 복사 / ✏️ 이름 변경(F2) / 🗑️ 삭제(Delete)` (독립카드 = 이름변경/삭제만).
- **파일 단축키(기존 유지)**: `F2` 이름변경 · `Delete` 삭제(다중) · `Enter` 열기 · 더블클릭 열기. 출처 `gui/dialogs.py:497~`.
- **🔍 미리보기**: PDF 썸네일 팝업(`_show_thumbnail_popup`).

### 4.9 우측 핵심 패널 (FileManagerWidget) — 절대 보존
> 정산 탭의 심장. 중앙에서 정리/병합 → 여기로 흐름.
1. **① 정리 폴더** (`list_target`) — 정리/병합 결과 폴더 목록. 클릭 → 내용 표시.
2. **② 폴더 내용** (`list_widget`, 드래그&드롭) — 선택 폴더의 파일, 추가/수정.
3. **하단 버튼**: `↓ 수입` · `↑ 수출` · `🔍 검색` — 서버로 파일 이동.
- 적용 위치: `gui/panels.py` FileManagerWidget. 재스타일만(구조 유지).

### 4.10 파일 뷰 (그룹/파일 토글의 "파일")
- 선택 폴더의 파일을 **탐색기형 리스트**(이름·크기·수정일).
- 상단 툴바 없음 → 기존 단축키(F2/Delete/Enter/우클릭) 사용. 하단에 단축키 힌트.

---

## 5. 구현 매핑 (어디를 고치나)

| 디자인 요소 | 파일 | 작업 | 난이도 |
|------|------|------|:---:|
| 색·간격·네온 제거 | `gui/claude_theme.py`(확장), `gui/styles.py`(네온 제거) | QSS 정리, 하드코딩 색 → 토큰화 | 🟢 |
| 타이틀바·T 로고 감시 | `gui_jarvis.py` (`title_bar`, `brand_mark`) | 마크 클릭→글로우+감시 토글 | 🟢 |
| 네비 레일 | `gui_jarvis.py` `_create_vertical_navbar` | 슬림 재스타일 | 🟢 |
| 그룹 → 얇은 행 | `gui/dialogs.py` GroupCard/IndependentCard | 행 형태로 재구성 | 🟠 |
| 카드 → 팝업 모달 | 신규 `QDialog` + 기존 body 재사용 | **가장 큰 구조 변경** | 🟠 |
| 병합 매핑/검증/우클릭/미리보기 | `gui/dialogs.py`, `core/validator.py`, `core/file_processor.py` | 기존 기능 재스타일 | 🟡 |
| 우측 핵심 패널 | `gui/panels.py` FileManagerWidget | 재스타일(구조 유지) | 🟢 |

---

## 6. 구현 순서 (안전하게, 점진적)

> 거대 파일(dialogs.py 5천 줄 등) + 테스트 없음 → **한 번에 갈아엎지 않는다.** 팀 3명이 계속 쓰면서 교체.

1. **0단계 (권장 안전망)** — 핵심 로직(파일명·그룹핑·병합순서·금액검증)에 최소한의 특성화 테스트. 선택이지만 권장.
2. **1단계 — 디자인 토큰 정리**: `claude_theme.py` 단일화, `styles.py` 네온 제거, 하드코딩 색 토큰으로 치환. (눈에 보이는 변화 시작)
3. **2단계 — 창/타이틀바/네비**: 슬림화, T 로고 감시.
4. **3단계 — 정산 탭 그룹 리스트 + 카드 팝업**: 얇은 행 → 팝업(수입/수출/독립). 우측 핵심 패널 재스타일.
5. **4단계 — 파일 뷰 · 우클릭 · 미리보기 · 검증 표시** 마감.
6. **5단계 — 나머지 탭**(보고/일정/설정) 동일 토큰으로 적용.

---

## 7. 범위 밖 / 다음 단계

- **다른 탭 디자인 미정**: 보고 · 일정 · 설정. (통관 탭은 현재 GUI상 "Coming Soon" — 새로 설계 가능)
- **동작/로직 항목**(정적 시안엔 없지만 구현 시 반영): 액션 버튼 상태머신, 호버 글로우+chevron 회전, 칩 클릭 해당없음 토글(config 저장), 드래그 아웃/드롭, 백그라운드 분석 스레드, 병합 후 빈 폴더 정리, 확인·이름변경·누락파일 다이얼로그.
- **HTML↔PyQt 차이 주의**: QSS는 flexbox 없음(레이아웃 위젯 사용), 애니메이션/글로우는 Qt 방식, 진짜 블러는 추가 작업.

---

## 8. 변경 이력
- 2026-06-28: 정산 탭 디자인 확정(시안 반복 합의) — 차분한 다크 통일 / 얇은 리스트 / 카드 팝업 / 수입·수출·독립 카드 / 병합 매핑 / 2단계 금액검증 / 우측 핵심 패널 보존 / T 로고 감시. 본 명세서 작성.
